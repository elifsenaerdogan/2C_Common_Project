import type { Meta } from '@storybook/react';
import { AtomShowMore } from './atom-show-more';

const Story: Meta<typeof AtomShowMore> = {
  component: AtomShowMore,
  title: 'Atoms/ShowMore',
};
export default Story;

export const Primary = {
  args: {},
};
