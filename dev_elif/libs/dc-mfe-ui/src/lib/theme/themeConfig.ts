import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#52c41e',
  },
  components: {
    Button: {
      borderRadius: 10
    }
  }
};

export default theme;