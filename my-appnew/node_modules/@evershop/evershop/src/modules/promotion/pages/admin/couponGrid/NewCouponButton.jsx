import PropTypes from 'prop-types';
import React from 'react';
import Button from '@components/common/form/Button';
import {useTranslation} from "react-i18next";
import '../../../../i18n'
import Layout from "@evershop/evershop/src/modules/cms/pages/frontStore/all/Layout";

export default function NewCouponButton({ newCouponUrl }) {
  const {t, i18n} = useTranslation()
  return <Button url={newCouponUrl} title={t('newcoupon')} />;
}

NewCouponButton.propTypes = {
  newCouponUrl: PropTypes.string.isRequired
};

export const layout = {
  areaId: 'pageHeadingRight',
  sortOrder: 10
};

export const query = `
  query Query {
    newCouponUrl: url(routeId: "couponNew")
  }
`;
